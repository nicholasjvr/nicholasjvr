// Legacy sides-preview strip (removed from widget layout Apr 2026 — 3D view makes it redundant).
// Kept initialized by planning2d/index.js for compatibility only.

export function initSidesPreview(/* ctx */) {}

export function renderSidesPreview() {
  /* Side preview markup removed — no-op */
}
