---
// The old WebGL orb lived here. Replaced with a live build-status board, which
// fits the build-log identity far better than a decorative 3D ornament.
import StatusBoard from './StatusBoard.astro';
---

<StatusBoard />
